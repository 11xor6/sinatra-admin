// Navigation Menu Tree
jQuery(document).ready(function($) {
    $('#nav-menu').dcAccordion(
            {
                showCount: true,
                speed: 'slow',
                autoClose: 'false'
//                autoExpand: true,


            });
});
